# 保密：业务策略与品牌记忆不进入公开仓库（已移入私有仓库）
运营总体规划*.md
memory/

# 依赖与系统文件
node_modules/
.DS_Store
*.log

# 编辑器临时文件
*.fileloc


# 公众号文章源文件：保持 untracked，由镜像脚本单独备份到私有仓
content-source/

# 本地产物输出（md-preview 生成的 HTML 预览等，仅供本机阅读）
out/

# WorkBuddy 会话数据（automations/ + memory/，含私密笔记，绝不进公开仓）
.workbuddy/

# 历史归档资产：本地磁盘保留，但不进版本库
# 原因：archive/ 已达 213M（0822老源94M + 风格试验61M + 地图迭代53M + v4产物5M），
#       进 git 会把 .git 撑到 300M+。这些是历史试验资产，本地保留即可，丢了不影响当前工作。
archive/
