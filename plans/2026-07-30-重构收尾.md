# 比特币学习地图 · 编号对调 + 滚动高亮 + 密码学页重建

日期：2026-07-30
工作目录：/Users/mac/Desktop/宝盒知识库/比特币学习地图

## 目标
1. 修正学习路径的章节编号逻辑（经济学篇与技术篇编号相反）
2. 全面检查页面逻辑，修正不合逻辑之处
3. 左侧"本页导航"总览随阅读位置高亮（scroll-spy）
4. 重做"密码学基石"页（用户评估为质量不达标）

## 完成内容

### 1. 章节编号对调（经济学 ↔ 技术）
- 原：`03-technology`（9页）、`04-economics`（空）
- 改后：`03-economics`（空，待建）、`04-technology`（9页）
- 执行：目录重命名 + 文件 `03-0X→04-0X` + 全站 14 个 html 内链接改写 + `index.html` 投资者模式占位链接同步 + `shared/nav.js` 映射键对调
- 校验：对调后全站仅剩 2 条预期内断链（tools 占位页），已修复

### 2. 滚动高亮 scroll-spy（新增）
- 在 `shared/nav.js` 增加 `initSectionScrollSpy()`：用 IntersectionObserver 监测各 section，读到哪节就给左侧 `.sidebar-nav` 对应锚点加 `.active`（CSS 已有橙色高亮样式）
- 在 `initNav()` 中调用
- 该高亮依赖"侧栏锚点 `#id` 与正文 `<section id>` 一一对应"

### 3. 密码学页 04-01-cryptography.html 整体重建
问题诊断（原页）：
- 各 `<section>` 缺 `id` → 侧栏锚点失效、scroll-spy 无法工作
- 无 `.two-col` 包裹 → 侧栏未与正文并排（布局错位）
- 用了未定义的 `.page-header`（CSS 无样式，裸渲染）
- 图3（secp256k1 曲线 SVG）错放在"数字签名"节，本属"椭圆曲线"节
- 同时存在自定义 `multi-perspective` 与共享 `perspective-card` 两套冗余视角
- 一个 `<p` 标签缺 `<`（畸形）

重建要点（对标 04-03-transactions.html 模板）：
- 新 BTCMap 导航 + `page-hero` + `reading-progress` + `two-col`（sidebar + main-content/article-body）
- 为每个 section 补 `id`（intro/hash/digital-sig/ecc/perspectives/labs/summary），与侧栏锚点 1:1 对应
- 3 个 SVG（哈希/签名/椭圆曲线）全部保留并归位正确章节
- 保留哈希计算器 + 签名演示互动（Web Crypto API）
- 删除冗余 `multi-perspective`，仅保留标准 `perspective-card`
- 修正畸形标签
- 给 `nav.js` 的 `PERSPECTIVE_CONFIG` 补 6 个 crypto 视角配色（dev/security/user/math/historian/analogy），使视角卡片正确着色

### 4. 主页悬空工具链接修复
- `index.html` 两个工具卡（总量理解器、HODL 模拟）指向不存在的 `tools/supply-calculator.html` / `tools/hodl-simulator.html`（用 onclick location.href，不在 href 检查范围）
- 改指向 `tools/index.html`（工具总览，已存在），消除 404 死链

## 校验结果
- 全站断链（含 href 与 onclick）：✅ 无
- `node --check shared/nav.js`：语法 OK
- 04-01 结构：page-hero / article-wrap / two-col / sidebar-nav / perspective-card / reading-progress 全部存在
- 04-01 标签平衡、3 SVG 保留、7 个侧栏锚点全部有对应 id
- 当前目录：00-overview, 01-philosophy(4), 02-basics(8), 03-economics(空), 04-technology(9), 05-ecosystem(空)

## 仍待办（非本次范围）
- `03-economics`（经济学篇）、`05-ecosystem`（生态篇）为空目录，待后续规划填充
- `tools/supply-calculator.html`、`tools/hodl-simulator.html` 实际工具页未建（目前入口指向工具总览）
